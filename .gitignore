node_modules/
.next/
next-env.d.ts
dist/
build/
coverage/
test-results/
playwright-report/
.env
.env.*
!.env.local.example
*.log
.DS_Store

# Claude worktrees (obreros de la fábrica, nunca commitear)
.claude/worktrees/
.factory/
*.tsbuildinfo

# Cache del CLI de Supabase (versión, refs): nunca se commitea
supabase/.temp/

# Líneas base visuales de Playwright: sólo Linux es vinculante (CI la genera
# y la commitea). Windows se regenera localmente y es sólo informativo
# (issue #58); versionarla reintroduce el conflicto binario entre PRs.
tests/ui.spec.ts-snapshots/*-win32.png
